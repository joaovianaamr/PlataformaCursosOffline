# ============================================
# Plataforma de Cursos Offline - Variáveis de Ambiente
# ============================================
# 
# INSTRUÇÕES:
# 1. Copie este arquivo para .env: cp .env.example .env
# 2. Ajuste os valores conforme necessário
# 3. NUNCA commite o arquivo .env (ele está no .gitignore)
#
# SINTAXE:
# - Use ${VARIAVEL:-valor_padrao} no docker-compose.yml
# - Se a variável não existir, o valor padrão será usado
# ============================================

# ============================================
# PostgreSQL - Configurações do Banco de Dados
# ============================================
POSTGRES_USER=plataforma
POSTGRES_PASSWORD=plataforma123
POSTGRES_DB=plataforma_cursos
# POSTGRES_PORT=5432  # Não exposta externamente por segurança

# ============================================
# Backend - Configurações da Aplicação Spring Boot
# ============================================
# Nome da aplicação (usado em spring.application.name)
APP_NAME=Plataforma de Cursos Offline

# Porta do backend (padrão: 8080)
BACKEND_PORT=8080

# JWT - Configurações de Autenticação
# IMPORTANTE: Altere em produção para uma chave segura (mínimo 256 bits)
JWT_SECRET=your-secret-key-change-in-production-min-256-bits
# Tempo de expiração do token em milissegundos (padrão: 24 horas = 86400000)
JWT_EXPIRATION=86400000

# Caminho para os vídeos (no host)
# No Docker: mapeia para /videos dentro do container
VIDEOS_PATH=./videos

# ============================================
# Frontend - Configurações do React
# ============================================
# Porta do frontend (padrão: 3000)
FRONTEND_PORT=3000

# URL da API backend (usado pelo frontend para fazer requisições)
VITE_API_URL=http://localhost:8080/api

# ============================================
# Notas de Segurança
# ============================================
# - POSTGRES_PASSWORD: Use uma senha forte em produção
# - JWT_SECRET: Gere uma chave aleatória segura (ex: openssl rand -base64 32)
# - O PostgreSQL não expõe a porta 5432 externamente por padrão
#   Para acessar: docker exec -it plataforma-postgres psql -U plataforma -d plataforma_cursos
